#include <stdio.h>

int main() {
    int num1, num2, sum;

    scanf("%d", &num1);
    scanf("%d", &num2);

    // Calculate the sum
    sum = num1 + num2;

    // Display the result
    printf("%d\n", sum); // Use format string to print the integer

    return 0;
}
